const m = require('../Stats')

let roots = {
        savHid:()=>m.find({}),
        savePerClick:()=>m.find({}),
        saveUnit:()=>m.find({}),
        upgradeLvl:()=>m.find({}),
    }

module.exports = roots;